import Header from "../components/Header/Header.tsx";

function Lists() {

    const newList = () => {
    }
  return (
    <main>
        <Header title="Lists" buttonClick={newList} />
    </main>
  )
}

export default Lists